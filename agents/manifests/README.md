# Agent manifests

Secrets-safe ACP agent baseline manifests migrated from the active acpx/Pi configuration.

`acpx-baseline.json` records default acpx settings plus the agent command/argument templates needed to run packaged Pi prompt agents without embedding local absolute credential paths or API keys.

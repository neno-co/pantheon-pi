#!/bin/sh
set -eu
SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
PACKAGE_ROOT=$(CDPATH= cd -- "$SCRIPT_DIR/../.." && pwd)
AGENT_NAME=$(basename -- "$0")
export PANTHEON_MAIN_AGENT="$AGENT_NAME"
export PI_SKIP_VERSION_CHECK="${PI_SKIP_VERSION_CHECK:-1}"
PROMPT_FILE="$PACKAGE_ROOT/agents/prompts/$AGENT_NAME.md"
if [ ! -f "$PROMPT_FILE" ]; then
  echo "pantheon-pi: missing prompt file: $PROMPT_FILE" >&2
  exit 127
fi
if [ -n "${PANTHEON_PI_MODEL:-}" ]; then
  exec pi --model "$PANTHEON_PI_MODEL" --thinking off --tools read,grep,find,ls --system-prompt "$(cat "$PROMPT_FILE")" "$@"
fi
exec pi --model nebius/nvidia/nemotron-3-super-120b-a12b --thinking off --tools read,grep,find,ls --system-prompt "$(cat "$PROMPT_FILE")" "$@"
